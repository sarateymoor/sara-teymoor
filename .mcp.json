{
  "mcpServers": {
    "airtable": {
      "command": "npx",
      "args": ["-y", "@anthropic/claude-airtable-mcp"],
      "env": {
        "AIRTABLE_API_KEY": "${AIRTABLE_API_KEY}"
      }
    }
  }
}
